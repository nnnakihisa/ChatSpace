.side-bar
  .side-bar__user-name
    .side-bar__user-name__top__item
      .side-bar__user-name__top__item-name
        = current_user.name
      .side-bar__user-name__top__item-icons
      = link_to new_group_path, class: "icons__btn" do
        = icon('far', 'edit')
      = link_to edit_user_path(current_user), class: "icons__btn" do
        = icon('fab', 'bitcoin')
  .side-bar__group-list
    - current_user.groups.each do |group|
      = link_to '#', class: "side-bar__group-list__groups" do
        .side-bar__group-list__groups-name
          = group.name
        .side-bar__group-list__groups-message
          まだメッセージがありません

